//ВЫЧИСЛИТЬ ВЫРАЖЕНИЕ//
#include <iostream>
#include <cmath>
using namespace std;
 int main()
 {
    float a=45,b=45,c=90,G;
    G = (sin(a*a) * sin(a*a) + sin(b*b) * sin(b*b)) / (sin(c*c) * sin(c*c));
    cout<<G;
     system("PAUSE>>void");
 }
